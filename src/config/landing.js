/**
 * Login / register landing UI switch.
 *
 * Set to `true` to restore the previous Theme Studio–era card layout
 * from `src/pages/Auth/Landing.classic.js` (exact pre-redesign UI).
 *
 * Default: `false` → redesigned landing in `Landing.js`.
 */
export const USE_LANDING_CLASSIC = false;
