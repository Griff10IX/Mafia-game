# Secrets - never commit
.env
**/.env
*.env
!*.env.example

# Kill bot — keep on this machine only; never commit, push, or deploy (credentials + automation).
MafiaKillBot/

# Dependencies
node_modules/

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.pytest_cache/
.ruff_cache/
*.egg-info/
.eggs/

# Build / IDE
build/
frontend/build/
.DS_Store
*.log
logs/

# Backups
backups/
*.tar.gz
backup_*/

# MongoDB dumps
dump/

# User-uploaded files (image host, etc.)
backend/uploads/
